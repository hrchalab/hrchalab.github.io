---
layout: null
sitemap:
  exclude: 'yes'
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for post in site.posts %}
    {% if post.published != false %}
    <url>
      <loc>{{ site.url }}{{ post.url }}</loc>
      <lastmod>{{ post.sitemap.lastmod | default: post.date | date: "%Y-%m-%d" }}</lastmod>
      <changefreq>{{ post.sitemap.changefreq | default: 'monthly' }}</changefreq>
      <priority>{{ post.sitemap.priority | default: 0.5 }}</priority>
    </url>
    {% endif %}
  {% endfor %}
  {% for page in site.pages %}
    {% if page.sitemap.exclude != "yes" %}
    <url>
      <loc>{{ site.url }}{{ page.url | remove: "index.html" }}</loc>
      <lastmod>{{ page.sitemap.lastmod | default: page.date | date: "%Y-%m-%d" }}</lastmod>
      <changefreq>{{ page.sitemap.changefreq | default: 'monthly' }}</changefreq>
      <priority>{{ page.sitemap.priority | default: 0.3 }}</priority>
    </url>
    {% endif %}
  {% endfor %}
</urlset>
